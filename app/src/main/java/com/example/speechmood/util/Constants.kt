package com.example.speechmood.util

object Constants {
    const val PackageName: String = "com.example.speechmood"

    object IntentNames {
        const val StartTime: String = "$PackageName.EXTRA_START_TIME"
        const val Labels: String = "$PackageName.LABELS"
        const val Timestamps: String = "$PackageName.TIMESTAMPS"
        const val FlattenedScores: String = "$PackageName.FLATTENED_SCORES"
    }
}
